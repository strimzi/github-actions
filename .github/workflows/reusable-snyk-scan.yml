name: Snyk Security Scan Pipeline

on:
  workflow_call:
    inputs:
      scanMaven:
        description: "Run Maven dependency scan"
        required: false
        type: boolean
        default: true
      scanContainers:
        description: "Run container image scan"
        required: false
        type: boolean
        default: false
      containerArtifact:
        description: "Name of container archive artifact to download (e.g., containers-operators-amd64)"
        required: false
        type: string
        default: ""
      containerArtifactRunId:
        description: "Workflow run ID to download the container artifact from"
        required: false
        type: string
        default: ""
      cvssThreshold:
        description: "CVSS score threshold — fail if any CVE scores above this"
        required: false
        type: string
        default: "8.0"
      javaVersion:
        description: "Java version to use for build"
        required: false
        type: string
        default: "21"
      monitor:
        description: "Run 'snyk monitor' to upload results to the Snyk dashboard"
        required: false
        type: boolean
        default: false
      projectPrefix:
        description: "Project prefix for Snyk dashboard (e.g., 'strimzi')"
        required: true
        type: string
      ref:
        description: "Git ref to checkout"
        required: false
        type: string
        default: ""
    secrets:
      SNYK_TOKEN:
        required: true

permissions:
  contents: read
  security-events: write
  actions: read

jobs:
  snyk-maven:
    name: Snyk Maven Scan
    if: ${{ inputs.scanMaven }}
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: ${{ inputs.ref || '' }}

      - name: Setup Java and Maven
        uses: strimzi/github-actions/.github/actions/dependencies/setup-java@main
        with:
          javaVersion: ${{ inputs.javaVersion }}

      - name: Install yq
        uses: strimzi/github-actions/.github/actions/dependencies/install-yq@main

      - name: Restore Maven cache
        uses: actions/cache/restore@v5
        with:
          path: ~/.m2/repository
          key: maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            maven-

      - name: Build Maven project
        shell: bash
        run: mvn -B -DskipTests -Dmaven.javadoc.skip=true clean install

      - name: Run Snyk Maven scan
        uses: strimzi/github-actions/.github/actions/security/snyk-maven-scan@main
        with:
          cvssThreshold: ${{ inputs.cvssThreshold }}
          monitor: ${{ inputs.monitor }}
          projectPrefix: ${{ inputs.projectPrefix }}
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

  snyk-containers:
    name: Snyk Container Scan
    if: ${{ inputs.scanContainers }}
    runs-on: ubuntu-latest
    timeout-minutes: 60
    outputs:
      images: ${{ steps.scan.outputs.images }}
    steps:
      - name: Install Docker
        uses: strimzi/github-actions/.github/actions/dependencies/install-docker@main

      - name: Run Snyk container scan
        id: scan
        uses: strimzi/github-actions/.github/actions/security/snyk-container-scan@main
        with:
          containerArtifact: ${{ inputs.containerArtifact }}
          runId: ${{ inputs.containerArtifactRunId }}
          cvssThreshold: ${{ inputs.cvssThreshold }}
          monitor: ${{ inputs.monitor }}
          projectPrefix: ${{ inputs.projectPrefix }}
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

  snyk-container-upload-sarif:
    name: Upload Container SARIF (${{ matrix.image }})
    if: ${{ inputs.scanContainers && needs.snyk-containers.result == 'success' }}
    needs: snyk-containers
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      matrix:
        image: ${{ fromJson(needs.snyk-containers.outputs.images) }}
    steps:
      - name: Download SARIF artifacts
        uses: actions/download-artifact@v7
        with:
          name: snyk-container-sarif-${{ inputs.projectPrefix }}
          path: sarif-download

      - name: Upload SARIF to GitHub Code Scanning
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: sarif-download/${{ matrix.image }}
          wait-for-processing: true
